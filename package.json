{
  "name": "student-system-frontend",
  "version": "1.0.0",
  "description": "Student management system frontend - static files",
  "private": true,
  "scripts": {
    "build": "echo 'No build needed - dist already committed to repo'"
  },
  "engines": {
    "node": ">=14"
  }
}
